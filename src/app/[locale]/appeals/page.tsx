import { AppealsView } from "@/views/team/AppealsView";

// Route trong app/ luôn giữ MỎNG — chỉ render View tương ứng, không chứa logic.
export default function AppealsPage() {
  return <AppealsView />;
}
