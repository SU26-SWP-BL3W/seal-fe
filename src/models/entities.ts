// ─── Domain entities ──────────────────────────────────────────────────────────
// Khai báo type cho các entity nghiệp vụ dùng chung nhiều feature (User, Event,
// Team, EventRole...) tại đây. Type CHỈ dùng riêng 1 feature thì khai trong
// repository của feature đó, không khai ở đây.
//
// Cố tình để trống khi scaffold — port/định nghĩa lại sạch theo đúng contract
// BE hiện tại, không copy nguyên khối từ repo cũ (tránh kéo theo field/giả định
// đã lệch backend).
